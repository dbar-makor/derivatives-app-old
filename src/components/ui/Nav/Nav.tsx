import React, { useState } from 'react';

import NavView from './Nav.view';

const Nav = () => {
  return (
    <NavView />
  );
};

Nav.displayName = 'Nav';
Nav.defaultProps = {};

export default Nav;